const slider = document.querySelector('.slider');

function slideLeft() {
  slider.scrollBy({
    left: -320,
    behavior: 'smooth'
  });
}

function slideRight() {
  slider.scrollBy({
    left: 320,
    behavior: 'smooth'
  });
}
function slideLeft() {
  slider.scrollBy({
    left: -320,
    behavior: 'smooth'
  });
}

function slideRight() {
  slider.scrollBy({
    left: 320,
    behavior: 'smooth'
  });
}